name: publish

on:
  workflow_dispatch:
  push:
    branches: ['release']

env:
  REGISTRY: ghcr.io
  IMAGE_BASE: ghcr.io/bozhidar-a/agro_calc
  NEXT_PUBLIC_HOST_URL: ${{ secrets.NEXT_PUBLIC_HOST_URL }}

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run Tests
        run: yarn test:all

      - name: Set build vars
        run: |
          echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_ENV
          echo "BRANCH_NAME=${GITHUB_REF_NAME}" >> $GITHUB_ENV
          echo "DOCKER_IMG_TAG=docker-publish-run-${GITHUB_RUN_NUMBER}" >> $GITHUB_ENV
          echo "LOG_BUILD_INFO=1" >> $GITHUB_ENV # Set to 0 to disable build info logging

      - name: Login to GitHub Container Registry (ghcr.io)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_BASE }}:latest
            ${{ env.IMAGE_BASE }}:${{ env.SHORT_SHA }}
            ${{ env.IMAGE_BASE }}:docker-publish-run-${{ github.run_number }}
          build-args: |
            NEXT_PUBLIC_HOST_URL=${{ secrets.NEXT_PUBLIC_HOST_URL }}
            NEXT_PUBLIC_COMMIT_SHA=${{ env.SHORT_SHA }}
            NEXT_PUBLIC_BRANCH_NAME=${{ env.BRANCH_NAME }}
            NEXT_PUBLIC_DOCKER_IMG_TAG=${{ env.DOCKER_IMG_TAG }}
            NEXT_PUBLIC_LOG_BUILD_INFO=${{ env.LOG_BUILD_INFO }}
          labels: |
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ env.SHORT_SHA }}
